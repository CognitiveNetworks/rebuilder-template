# TEST_RESULTS.md — [SERVICE_NAME] Quality Gate Report

**Generated:** [DATE]
**Generated By:** QA Agent (independent verification)
**Build Phase:** Step 12 — QA Agent Standards Compliance Audit

---

## Tool Versions

| Tool | Version |
|------|---------|
| Python | 3.12.x |
| pytest | x.x.x |
| pylint | x.x.x |
| black | x.x.x |
| mypy | x.x.x |
| radon | x.x.x |
| vulture | x.x |
| pip-audit | x.x.x |
| interrogate | x.x.x |
| complexipy | x.x.x |

## Codebase Metrics

| Metric | Count |
|--------|-------|
| Source files | X |
| Source lines | X |
| Test files | X |
| Test lines | X |
| Source:Test ratio | X:1 |

---

## Core Gates

### 1. pytest — [PASS ✅ / FAIL ❌]

```
[paste pytest output summary line]
```

| Module | Stmts | Miss | Cover |
|--------|-------|------|-------|
| [add one row per module from coverage report] | | | |
| **TOTAL** | **X** | **X** | **X%** |

**Coverage gaps explained:**
- [module] (X%): [explanation — e.g., OTEL setup requires live collector, mocked infrastructure]

No module with testable logic is below 50%.

### 2. Linter (pylint) — [PASS ✅ / FAIL ❌]

```
$ pylint src tests --disable=import-error --fail-under=9.0
[paste score line]
```

### 3. Formatter (black) — [PASS ✅ / FAIL ❌]

```
$ black --check src/ tests/ --skip-string-normalization
[paste output]
```

### 4. Type Checker (mypy) — [PASS ✅ / FAIL ❌]

```
$ mypy src/app/ --ignore-missing-imports --disable-error-code=unused-ignore
[paste output]
```

---

## Extended Gates

### 5. Cyclomatic Complexity (radon cc) — [PASS ✅ / FAIL ❌]

```
$ radon cc src/ -a -nc
[paste output or "(no output — no function rated C or higher)"]
```

### 6. Maintainability Index (radon mi) — [PASS ✅ / FAIL ❌]

| File | Rating |
|------|--------|
| [one row per file] | A/B/C |

### 7. Dead Code (vulture) — [PASS ✅ / FAIL ❌]

```
$ vulture src/ --min-confidence 80
[paste output or "(no output — 0 findings)"]
```

### 8. Dependency Vulnerabilities (pip-audit) — [PASS ✅ / FAIL ❌ / ADVISORY ⚠️]

```
$ pip-audit
[paste output]
```

**Assessment:** [Explain findings — runtime vs dev-only? Action needed?]

### 9. Docstring Coverage (interrogate) — [PASS ✅ / FAIL ❌]

```
$ interrogate src/ -v
[paste RESULT line]
```

### 10. Duplicate Code (pylint) — [PASS ✅ / FAIL ❌]

```
$ pylint --disable=all --enable=duplicate-code src/
[paste score line]
```

### 11. Cognitive Complexity (complexipy) — [PASS ✅ / FAIL ❌]

```
$ complexipy src -mx 15
[paste summary line]
```

---

## Helm Gates

### 12. Helm Lint — [PASS ✅ / FAIL ❌ / NOT RUN ⚠️]

```
$ helm lint charts/
[paste output or note if Helm CLI unavailable]
```

### 13. Helm Template Render — [PASS ✅ / FAIL ❌ / NOT RUN ⚠️]

```
$ tests/test-helm-template.sh -all
[paste summary or note if Helm CLI unavailable]
```

---

## /ops/* Endpoint Contract Verification

> Independently test every endpoint using `TestClient` with lifespan (not by reading test files).

### Diagnostic Endpoints (GET)

| Endpoint | Status | Actual Fields |
|----------|--------|---------------|
| `/status` | [✅/❌] [code] | [fields or body] |
| `/health` | [✅/❌] [code] | [fields] |
| `/ops/status` | [✅/❌] [code] | [fields — must include `status`, `uptime_seconds`] |
| `/ops/health` | [✅/❌] [code] | [fields — must include `status`, dependency info] |
| `/ops/metrics` | [✅/❌] [code] | [fields — must include latency, traffic, errors, uptime] |
| `/ops/config` | [✅/❌] [code] | [fields — must include `service_name`] |
| `/ops/errors` | [✅/❌] [code] | [fields — must include total, recent] |
| `/ops/cache` | [✅/❌] [code] | [fields — must include entry count] |

### Remediation Endpoints (POST)

| Endpoint | Status | Behavior |
|----------|--------|----------|
| `/ops/loglevel` (valid) | [✅/❌] [code] | [describe response] |
| `/ops/log-level` (valid) | [✅/❌] [code] | [canonical alias] |
| `/ops/loglevel` (invalid) | [✅/❌] [code] | [must return 400] |
| `/ops/cache/flush` | [✅/❌] [code] | [describe response] |
| `/ops/cache/refresh` | [✅/❌] [code] | [describe response] |
| `/ops/circuits` | [✅/❌] [code] | [describe response] |

### Event Ingestion (if applicable)

| Scenario | Status | Result |
|----------|--------|--------|
| [Valid payload] | [✅/❌] [code] | [accepted/rejected] |
| [Missing required params] | [✅/❌] [code] | [accepted/rejected] |
| [Invalid security hash] | [✅/❌] [code] | [accepted/rejected] |

**Assessment:** [Note field naming deviations from generic contract, if any]

---

## Quality Gate Summary

| Gate | Threshold | Result | Status |
|------|-----------|--------|--------|
| pytest | 0 failures | X passed, 0 failed | [✅/❌] |
| Test coverage | ≥ 80% | X% | [✅/❌] |
| pylint (lint) | score ≥ 9.0 | X/10 | [✅/❌] |
| black (format) | All formatted | X/X unchanged | [✅/❌] |
| mypy (types) | 0 errors | X errors in X files | [✅/❌] |
| Cyclomatic complexity | No function ≥ C | [All A/B or list] | [✅/❌] |
| Maintainability index | All files A or B | [All A or list] | [✅/❌] |
| Dead code (vulture) | 0 findings | X findings | [✅/❌] |
| Dependency vulns | 0 runtime CVEs | X runtime, X dev-only | [✅/❌/⚠️] |
| Docstring coverage | ≥ 80% | X% | [✅/❌] |
| Duplicate code | < 3% | X% | [✅/❌] |
| Cognitive complexity | 0 issues | X issues | [✅/❌] |
| Helm lint | 0 errors | X errors | [✅/❌/⚠️] |
| Helm template render | dev/qa/prod | [renders / fails] | [✅/❌/⚠️] |

**Overall: X/14 gates PASS**

---

## Comparison with Developer Agent TEST_RESULTS.md

> If a prior developer or QA report exists, compare key metrics.
> If this is the first run, note "First QA pass — no prior report."

| Metric | Developer / Prior QA | Current QA | Match |
|--------|---------------------|------------|-------|
| Total tests | X | X | [✅/⚠️] |
| Coverage | X% | X% | [✅/⚠️] |
| TOTAL stmts | X | X | [✅/⚠️] |
| pylint score | X | X | [✅/⚠️] |
| Docstring coverage | X% | X% | [✅/⚠️] |
| Inline imports (src/) | X | X | [✅/⚠️] |
| `global` keyword (src/) | [present/0] | [present/0] | [✅/⚠️] |
| Suppression comments | X | X | [✅/⚠️] |

**Assessment:** [Explain any discrepancies — code changes between reports, different tool flags, etc.]

---

## Bugs Found and Fixed During Validation

| Bug | File | Description | Fix |
|-----|------|-------------|-----|
| [rule id] | [file.py] | [description] | [fix applied] |

> If none: "None. All gates passed on first run."

## Not Yet Tested

The following require running infrastructure that cannot be validated offline:

- **[Dependency]** — [what cannot be tested and why]
- **Docker container runtime** — `docker build` and `docker compose up` not executed in CI-less environment. Dockerfile validated by inspection.
- **OTEL collector** — Trace/metric/log export to OTEL collector requires running collector. Instrumentation wired in code; export paths not exercised.
- **Helm deployment** — [note if Helm CLI unavailable]

---

## Full template/skill.md Checklist Audit

> Walk **every section** of `template/skill.md` and verify each checkbox against the built
> service. Do not skip sections. Mark each item `[x]`, `[ ]`, `[N/A]`, or `[⚠️]`.
> For items marked `[⚠️]` or `[ ]`, include a brief explanation.

### Coding Practices (template/skill.md preamble)

> For each item, grep the source code and verify compliance.

- [ ] Do not shadow global variables or constants inside function calls
- [ ] Minimize global variables; pass state through function arguments, class instances, or DI — `grep -rn "^global \|^\s\+global " src/`
- [ ] All constants must be UPPER_CASE
- [ ] All imports must be top-level — `grep` for `import` inside function bodies (exclude `TYPE_CHECKING` guards)
- [ ] Do not introduce circular imports
- [ ] Resolve linter errors instead of ignoring them — `grep "# pylint: disable\|# type: ignore\|# noqa" src/`
- [ ] No `# type: ignore` comments anywhere
- [ ] Match expected types exactly — confirmed by mypy 0 errors
- [ ] Verify argument types against function signatures
- [ ] When integrating with external modules, read type hints
- [ ] Do not write equality checks that can never be true
- [ ] Match framework function signatures exactly
- [ ] Always parameterize generic types — `grep ": dict\b\|: list\b\|-> dict\b\|-> list\b" src/`
- [ ] Run `mypy` as type-mismatch safety net
- [ ] All functions, classes, and modules must have docstrings — confirmed by `interrogate`
- [ ] Use f-strings instead of `%` formatting or `.format()` — `grep "\.format(\|% [sd\"'(]" src/`

### Section 1: Dependency Management

- [ ] `pyproject.toml` defines runtime deps with pinned versions under `[project] dependencies`
- [ ] `pyproject.toml` defines dev deps under `[project.optional-dependencies] dev`
- [ ] Dev deps include: pytest, pytest-cov, pylint, black, mypy, complexipy, pip-tools, coverage[toml]
- [ ] `pip-tools` used for lock file generation (not `pip freeze`)
- [ ] `scripts/lock.sh` exists and matches template pattern
- [ ] `requirements.txt` committed — verify header says `autogenerated by pip-compile`
- [ ] `requirements-dev.txt` committed — verify header says `autogenerated by pip-compile`
- [ ] `lock.sh` pins pip, setuptools, pip-tools versions before compiling
- [ ] `[build-system]` requires `setuptools>=78` with `setuptools.build_meta` backend

### Section 2: Required Development Tooling

- [ ] `[tool.black]` section: `line-length = 88`, `target-version = ['py312']`, `skip-string-normalization = true`
- [ ] `[tool.mypy]` section: `ignore_missing_imports = true`
- [ ] `.pylintrc` `[MASTER]` init-hook adds current directory to sys.path
- [ ] `.pylintrc` `[MESSAGES CONTROL]` disable list includes: C0116, C0114, C0115, R0903, R0913, R0917, W0602, W1203, W1202, W0511, R0401, W0718
- [ ] `pytest.ini` exists with coverage config
- [ ] `norecursedirs` excludes `cntools_py3 cnlib`
- [ ] `env` sets `OTEL_SDK_DISABLED=true`
- [ ] `filterwarnings` ignores `DeprecationWarning`

### Section 3: Dockerfile Pattern

- [ ] Base image: `python:3.12-bookworm`
- [ ] CVE-pinned system packages
- [ ] Purge unnecessary packages
- [ ] Copy application source and requirements.txt
- [ ] Upgrade pip and setuptools to pinned versions
- [ ] Install requirements from `requirements.txt`
- [ ] Install cnlib (or N/A with justification)
- [ ] Set `PYTHONUNBUFFERED=1`
- [ ] Set `OTEL_EXPORTER_OTLP_COMPRESSION="gzip"`
- [ ] Run `opentelemetry-bootstrap`
- [ ] Copy `entrypoint.sh` and `environment-check.sh` to `/usr/local/bin/` and `chmod +x`
- [ ] `EXPOSE 8000`
- [ ] Non-root user: `containeruser` with UID/GID `10000`
- [ ] `HEALTHCHECK` on `http://localhost:8000/status`
- [ ] `ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]`

### Section 4: entrypoint.sh Pattern

- [ ] `#!/bin/bash` with `set -eu pipefail`
- [ ] Sources `environment-check.sh`
- [ ] Creates AWS config file
- [ ] Exports and logs `OTEL_PYTHON_AUTO_INSTRUMENTATION_ENABLED`
- [ ] Conditional OTEL setup
- [ ] Starts uvicorn with correct module path

### Section 5: environment-check.sh Pattern

- [ ] `#!/bin/bash` (no `set -e`)
- [ ] Defines `always_required_vars` array
- [ ] Defines optional variable groups as needed
- [ ] `ENV` validated first
- [ ] `TEST_CONTAINER=true` logic (only `ENV=dev`, disables OTEL)
- [ ] Non-test path checks all required + conditional variable groups
- [ ] OTEL debug mode when `LOG_LEVEL=debug`
- [ ] Final check reports unset variables and exits non-zero

### Section 6: Application Structure

- [ ] OTEL TracerProvider, MeterProvider, and LoggerProvider configured
- [ ] LoggerProvider with `BatchLogRecordProcessor` and `OTLPLogExporter`
- [ ] MeterProvider with `PeriodicExportingMetricReader` and `OTLPMetricExporter`
- [ ] TracerProvider with `BatchSpanProcessor` and `OTLPSpanExporter`
- [ ] `Resource.create({"service.name": service_name})`
- [ ] `FastAPIInstrumentor.instrument_app(app)`
- [ ] `compute_valid_log_level()` function
- [ ] `configure_logging()` function (or N/A if cnlib removed)
- [ ] `lifespan` async context manager
- [ ] `create_app()` factory function
- [ ] `main.py` minimal entry point
- [ ] `APIRouter()` instances in route files
- [ ] `/status` returns `PlainTextResponse('OK')`
- [ ] `/health` with dependency checks
- [ ] OTEL tracer via `trace.get_tracer(__name__)` in API routers
- [ ] OTEL metrics counter for request counting
- [ ] `log_request_middleware` — logs requests, skips `/status` and `/health`

### Section 7: GitHub Workflows

- [ ] CI pipeline file (commit.yml or ci.yml)
- [ ] `comment-build-push-container.yaml`
- [ ] `master-build-push-container.yaml`
- [ ] `pre-release-build-push-container.yaml`
- [ ] `enforce-gitflow.yaml`
- [ ] `pod-identity-manager.yml`
- [ ] CI jobs: black, pytest, pylint, complexipy, mypy, helm_lint, helm_template, helm_unittest, build-and-test-container
- [ ] Multi-arch builds (`linux/amd64,linux/arm64`)
- [ ] Image tag format: `{app}-{branch}-{sha}`
- [ ] `.github/scripts/pod-identity-generator/` directory
- [ ] `.github/scripts/workflow-runner.sh`

### Section 8: Git Hooks

- [ ] `hooks/pre-commit` exists with all quality gates in order
- [ ] Hook enabled via `git config core.hooksPath hooks`
- [ ] Colored output, exits non-zero on failure

### Section 9: Helm Charts

- [ ] All 18+ template files present (list each)
- [ ] `charts/Chart.yaml` — correct name, apiVersion v2
- [ ] `charts/values.yaml` — environment config, resources, probes
- [ ] `charts/tests/` directory with unittest YAML files
- [ ] `tests/test-helm-template.sh` covers dev, qa, prod, PR

### Section 10: Required Files Checklist

- [ ] `.actrc`, `.gitignore`, `.pylintrc`, `.python-version`
- [ ] `Dockerfile`, `README.md`, `catalog-info.yaml`
- [ ] `entrypoint.sh`, `env.list`, `environment-check.sh`
- [ ] `hooks/pre-commit`, `monitored-paths.txt`
- [ ] `pyproject.toml`, `pytest.ini`
- [ ] `requirements.txt`, `requirements-dev.txt`
- [ ] `scripts/lock.sh`, `tests/test-helm-template.sh`

### Section 11: Environment Variables

- [ ] Always required: ENV, LOG_LEVEL, AWS_REGION, TEST_CONTAINER, SERVICE_NAME, OTEL_PYTHON_AUTO_INSTRUMENTATION_ENABLED
- [ ] OTEL variables defined in `environment-check.sh`
- [ ] `env.list` matches template reference

### Section 12: Local Development

- [ ] Python 3.12 venv
- [ ] Install dev deps
- [ ] Install cnlib or local modules
- [ ] Run tests: `pytest`
- [ ] Run locally via Docker documented
- [ ] `curl /status` returns `OK`
- [ ] Git hooks documented

### Section 13: CVE Management

- [ ] `cves/` directory exists
- [ ] System packages pinned in Dockerfile
- [ ] pip and setuptools upgraded to CVE-safe versions

---

## Findings Summary

### Passed
- [List all passing items — tests, coverage, gates, files, endpoints]

### Coding Standards Gaps

> If gaps were found and fixed, show the resolution table.
> If this is a re-verification after fixes, show before/after.

| Gap | Status | Details |
|-----|--------|---------|
| [description] | [✅ Fixed / ❌ Open / ⚠️ Acceptable] | [explanation] |

### Critical Gaps
- [List or "None identified."]

### Important Gaps
- [List categorized by section — Coding Practices, Application Structure, Workflows, etc.]

### Advisory
- [Non-blocking items — dev-only CVEs, cosmetic issues]

### Minor Gaps
- [Field naming deviations, filename differences, etc.]

### Recommendations
1. [Numbered list of actionable items]
